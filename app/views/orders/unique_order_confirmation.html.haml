%h2 Thanks for shopping with us.
%h3 Here are your order details:
%h4 
  Order Number:# 
  = @order.id.to_s.rjust(7,"0") 
  %table
    %th
    %th Item
    %th Quantity
    %th Price
    %th Description
    %th Subtotal
    - @products.each do |line_item, product|
      %tr
        %td= image_tag "http://placekitten.com/100/100"
        %td= product.name
        %td= line_item.quantity
        %td= number_to_currency(product.price)
        %td= product.description
        %td= number_to_currency(line_item.subtotal)       
%h4
  Order Total:
  = number_to_currency(@order.total)


