= form_for @store_admin_store do |f|
  - if @store_admin_store.errors.any?
    #error_explanation
      %h2= "#{pluralize(@store_admin_store.errors.count, "error")} prohibited this store_admin_store from being saved:"
      %ul
        - @store_admin_store.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
