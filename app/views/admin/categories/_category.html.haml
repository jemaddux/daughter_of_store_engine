.row
  .large-12.columns
    .left
      %h2
        = link_to "#{category.name}: ", admin_category_path(category)
        = "#{category.products.count} items"
      .small-4.large-6.columns
        = category.description.to_s
      = link_to "Edit", edit_admin_category_path(category), class:'button right'
