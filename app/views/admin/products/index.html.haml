/ - @products.each do |product|
/   %h1= product.name
/   %p= product.description

/ Header and Nav
.row
  .large-12.columns
    .panel
      %a.button.right{:href => new_admin_product_path} New Product
      
      %h1 Active Products
/ End Header and Nav
.row
  / Nav Sidebar
  / This is source ordered to be pulled to the left on larger screens
  
  / Main Feed
  / This has been source ordered to come first in the markup (and on small devices) but to be to the right of the nav on larger screens

  .large-12.columns
    - @products.each do |product|
      .row
        .large-2.columns.small-3
          %img{:src => "http://placehold.it/80x80&text=[img]"}/
        .large-10.columns
          .left
            %h2= product.name
            %p= product.description
            /%a= link_to Category.find(product.category_id), categories_path
          .right
            %h2= "$ #{product.price}"
            %a.button.right{:href => "#"} Retire
            %a.button.right{:href => "#"} Edit

       
      / End Feed Entry
      