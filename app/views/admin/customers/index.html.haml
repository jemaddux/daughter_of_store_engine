%h1 Customer List

%table
  %tr
    %th Customer Name
    %th Customer id
    %th Email
    %th Creation Date
    %th
    %th
    %th

  - @customers.each do |customer|
    %tr
      %td= customer.username
      %td= customer.id
      %td= customer.email
      %td= customer.created_at
      %td= link_to 'Show', admin_customer_path(customer), class: "button tiny round"
      %td= link_to 'Destroy', admin_customer_path(customer.id), method: :delete, class: "button tiny round"
%br

= link_to 'New Order', new_admin_order_path, class: "button"
