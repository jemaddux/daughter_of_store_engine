%h1= "#{@store.name} Product Listing:"

.large-12.columns
  .row
    .large-12.columns
      = link_to "New Product", new_stocker_product_path(@store.path), class: "button right"
      %h1.admin-heading Products
.row
  .large-12.columns
    = render @products
